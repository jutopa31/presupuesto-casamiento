import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.presupuesto.casamiento',
  appName: 'Presupuesto Casamiento',
  webDir: 'dist'
};

export default config;
